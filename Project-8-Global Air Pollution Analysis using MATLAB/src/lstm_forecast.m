function lstm_forecast(~, ~, ~, ~)
% LSTM_FORECAST  Stub — visualization moved to visualize_data.m.
%   All 4 project figures are now generated in visualize_data().
%   This function is kept so main.m does not error.
disp("lstm_forecast: figures consolidated into visualize_data.");
end
